import 'package:flutter/material.dart';

class FortuneTodayScreen extends StatelessWidget {
  const FortuneTodayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('오늘의 운세 내용')),
    );
  }
}
