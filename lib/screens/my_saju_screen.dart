import 'package:flutter/material.dart';

class MySajuScreen extends StatelessWidget {
  const MySajuScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('내 사주조회 내용')),
    );
  }
}
